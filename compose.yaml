services:
  platania:
    build: .
    ports:
      - "8010:8010"
    env_file:
      - path: .env
        required: false
    volumes:
      - market-cache:/app/data/cache
    restart: unless-stopped

volumes:
  market-cache:

